# Full dataset publication rerun

Triggers the workflow after adding robust synchronization with the current `main` branch, so generated datasets and documentation can coexist without a non-fast-forward push failure.
