# Dataset Layout Migration

This branch exists only to trigger the workflow that republishes generated files into the organized `data/`, `models/`, `reports/`, and `artifacts/` directories. The migration output is committed directly to `main` by the workflow; this trigger branch does not need to be merged.
